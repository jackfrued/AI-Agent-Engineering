## 使用 LangChain 构建 AI 智能体

在前面的章节中，我们已经通过构建了一些简单的 AI 应用，其中的网页摘要助手、用户问题分类助手就是比较简单的智能体。但随着应用程序的功能变多、逻辑变复杂，你就会发现**代码变得不那么优雅了**。为此，我们将引入一个专门为 LLM 与 AI 智能体而生的工程化框架 **LangChain**，看看它如何帮助我们更系统的构造可维护的 AI 智能体应用。

### 1. 为什么需要 LangChain

编写智能体应用需要拼接和管理 Prompt、格式化大模型的输出、维护会话上下文环境，对于更复杂的智能体应用场景，我们还需要调用工具函数、构建工作流、处理多个 Agent 的通信等等，这些代码本身的逻辑并不复杂，但重复度很高，对于工程化开发几乎是不能够接受的。在这样的背景下，一个名为 LangChain 的框架应运而生，根据 LangChain 官方的介绍，这个框架能够帮助我们高效的构建由大语言模型（LLM）驱动的应用（这种应用就是智能体，也称为“LLM 应用”）。LangChain 的核心价值并不是减少开发 LLM 应用程序的代码量，而是帮我们**把 LLM 应用中反复出现的模式抽象为便捷的组件**，我们可以通过**组装这些组件来构造复杂的 LLM 应用**。

按照 LangChain 官方的介绍，使用 LangChain 将为我们带来以下好处：

1. **实时数据增强**。LangChain 可以轻松将大模型与多样化的数据源以及内外部系统连接起来，利用其封装好的代码库（包括：模型提供方、工具、向量数据库、检索器等），让大模型能够基于实时和外部数据进行推理与生成。
2. **模型互操作性**。在团队试验和评估不同模型的过程中，LangChain 可以帮助我们随时切换模型以找到最符合应用需求的方案。大模型目前仍然处于快速演进中，LangChain 提供的抽象层能够帮助你快速适配变化，而无需推倒重来，从而保持持续迭代的能力。
3. **快速原型开发**。借助 LangChain 模块化、组件化的架构，可以快速构建并迭代 LLM 应用。在不重新开发整体系统的情况下，灵活尝试不同方案和工作流，大幅缩短开发周期，加速从想法到落地的过程。
4. **产品级特性支持**。通过与 LangSmith 等工具的深度集成，LangChain 内置了监控、评估和调试等关键能力，帮助你部署稳定可靠的应用。同时，依托成熟的工程模式和最佳实践，让系统在规模化运行时依然具备高可靠性和可维护性。
5. **活跃的社区和生态**。LangChain 拥有丰富的集成方案、模板以及社区贡献的组件，开发者可以直接复用成熟能力。持续活跃的开源社区确保框架不断演进，帮助你始终紧跟最新的 AI 技术进展。

### 2. LangChain 生态体系

LangChain 框架本身可以独立使用，同时也能与 LangChain 生态中的其他产品无缝集成，为开发 LLM 应用提供完整的一站式工具链，其中包括：

1. **LangGraph**。使用 LangGraph 构建能够稳定处理复杂任务的智能体。作为底层的智能体编排框架，LangGraph 提供了高度可定制的架构、长期记忆能力以及人机协同（Human-in-the-Loop）工作流，并已在 LinkedIn、Uber、Klarna、GitLab 等企业的生产环境中得到验证。
2. **LangSmith**。用于智能体评估与可观测性建设。它可以帮助你调试性能不佳的 LLM 应用运行过程，评估智能体的执行轨迹，提升生产环境的可视化程度，并持续优化系统性能。
3. **LangSmith Deployment**。专为长时间运行、具备状态的工作流设计的部署平台，支持智能体的高效部署与弹性扩展。你可以在团队之间发现、复用、配置和共享智能体，并通过 LangSmith Studio 的可视化原型工具快速迭代。
4. **Deep Agents**。用于构建具备任务规划能力、可调用子智能体并能够利用文件系统完成复杂任务的高级智能体。

### 3. LangChain 快速上手

我们通过一些简单的示例带大家快速的了解 LangChain，我们先为项目安装一些必要的依赖项。

```bash
conda install langchain langchain-openai python-dotenv -c conda-forge
```

或

```bash
pip install langchain langchain-openai python-dotenv
```

#### 3.1 模型和消息

用原生的方式接入大模型代码往往因供应商不同需要编写不同的代码，LangChain 的 `BaseChatModel` 为访问不同的模型提供了一层**统一的抽象封装**。无论底层换成什么模型，你的顶层业务代码几乎不需要改。与此同时，LangChain 还对用户跟大模型对话的消息进行了封装，抽象出了三类核心消息对象：

1. `SystemMessage`（系统消息）：用于设定 AI 的角色、规矩和边界（最高优先级）。
2. `HumanMessage`（人类消息）：用户输入的文本（用户提示词）。
3. `AIMessage`（AI 消息）：大模型生成的返回内容，`AIMessage` 对象的属性非常丰富，如下所示：
    - `content`：这是消息的主体字段，通常是一个字符串，包含 AI 的文本回复。同时，它也支持多模态内容（如未类型化的对象列表或字典），允许在消息中直接传递图片、音频等复杂数据。
    - `tool_calls`：一个列表，包含了模型请求调用的工具信息。每个工具调用（`ToolCall`）通常是一个字典，包含工具名称（`name`）、传入参数（`args`）以及唯一的调用标识符（`id`）。
    - `usage_metadata`：以字典形式保存了令牌（Token）的计数信息，通常包括输入 Token 数（`input_tokens`）、输出 Token 数（`output_tokens`）以及总消耗量（`total_tokens`）。
    - `response_metadata`：包含与底层模型提供商相关的额外信息，例如模型名称（`model_name`）、完成原因（`finish_reason`）以及系统指纹等。

之前说过，访问 LLM 的 API Key 等敏感信息要放在 `.env` 文件中而不是直接写在代码中，我们还是使用 python-dotenv 库的函数 `load_dotenv` 从 `.env` 文件中加载这些内容到环境变量。下面，我们使用 LangChain 跟 LLM 进行一次简单的对话，代码如下所示。

```python
from dotenv import load_dotenv
from langchain_core.messages import SystemMessage, HumanMessage
from langchain_openai import ChatOpenAI

load_dotenv()
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0.2
)
msg = llm.invoke([
    SystemMessage(content='你是一个非常严谨的技术写作助手'),
    HumanMessage(content='用 200 字介绍什么是 AI 智能体'),
])
print(msg.content)
```

通过上面的代码，相信大家不难发现，我们跟大模型对话的时候，不需要关心 HTTP 细节；我们可以直接创建对应 LLM 的对象（上面代码中的`ChatOpenAI`）进行调用（`invoke`方法）；我们可以利用`SystemMessage`和`HumanMessage`来表示系统提示词和用户提示词；我们可以直接通过`invoke`方法返回对象（`AIMessage`对象）的属性来获得大模型生成的内容。

如果需要开启迭代器模式获取 LLM 返回的内容，只需要对上面的代码稍作修改，如下所示。

```python
from dotenv import load_dotenv
from langchain_core.messages import SystemMessage, HumanMessage
from langchain_openai import ChatOpenAI

load_dotenv()
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0.2,
)
gen = llm.stream([
    SystemMessage(content='你是一个非常严谨的技术写作助手'),
    HumanMessage(content='用 200 字介绍什么是 AI 智能体'),
])
for chunk in gen:
    print(chunk.content, end='', flush=True)
```

> **注意**：使用`ChatOpenAI`对象的`stream`方法会返回一个生成器对象，将该生成器对象置于`for`循环中会获得`AIMessageChunk`对象，通过`AIMessageChunk`对象的`content`属性就可以获得 LLM 响应的内容片段。

#### 3.2 提示词模板和LCEL

LangChain 提供了提示词模板便于我们动态组装和构造提示词，相较于 Python 原生的 f-string 只能做简单的字符串替换， LangChain 的 `ChatPromptTemplate` 对内能够防止我们自己把变量名写错导致程序崩溃，对外能够防止不怀好意的用户通过提示词调教和控制我们的大模型。我们可以使用 LangChain 的核心是 **LCEL **，它通过重载 Python 的 `|` 运算符，可以将不同的组件（模型、提示词、解析器、检索器等）编排为统一的链（Chain）。通过 LCEL，我们将模型和提示词模板组装成链来使用，代码写起来极度的舒适和优雅。

```python
from dotenv import load_dotenv
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI

load_dotenv()
prompt = ChatPromptTemplate.from_messages([
    ('system', '你是一个专业的英语翻译助手，擅长提供信达雅的翻译'),
    ('human', '请翻译下面的内容:\n{content}\n注意：只输出译文'),
])
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0.2
)
chain = prompt | llm
contents = [
    'The pen is in the box',
    'The box is in the pen',
    'The spirit is willing, but the flesh is weak',
]
results = chain.batch([{'content': content} for content in contents])
for result in results:
    print(result.content)
```

注意上面代码中的提示词模板`ChatPromptTemplate`，它允许在构造提示词是使用占位符。在调用模型的时候，我们可以根据实际需要传入内容替换提示词中的占位符，这是不是非常的方便。上面代码中更为重要的是 `chain = prompt | llm` 这一行，上面我们说过，LangChain 允许我们通过 `|` 运算符将各种组件组装到一起，这个叫做 LCEL。如果你知道 Unix 系统的管道操作（连接多个进程，将一个程序的标准输出直接作为另一个程序的标准输入，Unix 重要哲学之一），对这个运算符就一定不会感到陌生，可以将其理解为**提示词模板的输出（完整的提示词）就是大模型的输入**。这里的 `chain` 是 LangChain 中的`RunnableSequence`对象，它可以把多个 Runnable 对象串成一条可执行流水线，LangChain 中几乎几乎所有核心对象都是 Runnable 对象，包括提示词模板、模型、输出解析器、工具等，稍后我们都会见到的。

如果我们想实现多国语言翻译，可以对上面的代码稍加修改，如下所示。

```python
import itertools

from dotenv import load_dotenv
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI

load_dotenv()
prompt = ChatPromptTemplate.from_messages([
    ('system', '你是一个专业的翻译助手，擅长信达雅的翻译且只输出译文'),
    ('human', '请将下面的内容翻译成{language}：\n\n{content}\n，只输出译文'),
])
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0.2
)
chain = prompt | llm
contents = [
    'The pen is in the box',
    'The box is in the pen',
    'The spirit is willing, but the flesh is weak',
]
languages = ['中文', '日语', '德语', '俄语']
items = [{'content': content, 'language': language}
         for content, language in itertools.product(contents, languages)]
results = chain.batch(items)
for i, result in enumerate(results):
    print(result.content)
```

#### 3.3 输出解析器

如果我们对大模型输出的格式有要求，可以自行修改提示词，对大模型的输出做出约束。当然，更为便捷的方式是使用 LangChain 提供的输出解析器，我们这里先看一个简单的例子。

```python
from dotenv import load_dotenv
from langchain_core.output_parsers import JsonOutputParser
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from pydantic import BaseModel, Field


class TranslationResult(BaseModel):
    origin_language: str = Field(description='原文语言')
    origin_content: str = Field(description='原文内容')
    target_language: str = Field(description='译文语言')
    translation: str = Field(description='译文内容')
    style: str = Field(description='翻译风格')
    translator: str = Field(description='译者')


user_input = input('请输入要翻译的内容：')

load_dotenv()
parser = JsonOutputParser(pydantic_object=TranslationResult)
prompt = ChatPromptTemplate.from_messages([
    ('system', '你叫孙小美是一个专业的翻译助手，擅长提供信达雅的中文翻译。\n{format_instructions}'),
    ('human', '请翻译下面的内容:\n{content}'),
]).partial(format_instructions=parser.get_format_instructions())
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0.2
)
chain = prompt | llm | parser
result = chain.invoke({'content': user_input})
print(result)
```

> **说明**：上面代码创建了一个`JsonOutputParser`对象，它可以帮助我们将大模型的输出处理成 JSON 格式。为了描述这个 JSON 格式，我们定义了名为`TranslationResult`的类，并根据这个类创建了`JsonOutputParser`解析器对象。从上面的代码可以看出，输出解析器对象的`get_format_instructions`方法可以帮助我们产生约束输出格式的提示词，我们只需要将这段提示词加入到系统提示词中即可。在上面的代码中，我们通过提示词模板对象的`partial`方法，传入了这段内容，这个方法之所以叫 partial，表示我们只是把提示词模板中的部分占位符进行替换，跟 Python 中偏函数的味道是一样一样的。

运行程序，输入下面的内容：

```
Man is distinguished, not only by his reason, but by this singular passion from other animals, which is a lust of the mind, that by a perseverance of delight in the continued and indefatigable generation of knowledge, exceeds the short vehemence of any carnal pleasure.
```

程序运行结果如下所示：

```json
{'origin_language': 'English', 'origin_content': 'Man is distinguished, not only by his reason, but by this singular passion from other animals, which is a lust of the mind, that by a perseverance of delight in the continued and indefatigable generation of knowledge, exceeds the short vehemence of any carnal pleasure.', 'target_language': 'Chinese', 'translation': '人类之所以区别于其他动物，不仅在于其理性，更在于这种独特的精神渴求——一种心灵的欲望，它通过对知识持续不懈、乐此不疲的追求所获得的持久欢愉，超越了任何肉体快感的短暂激烈。', 'style': '典雅书面语', 'translator': '孙小美'}
```

#### 3.4 工具（函数调用）

下面，我们进入“智能体”的核心部分，引入工具。之前我们说过，大模型在理解和生成人类自然语言甚至编程语言上已经相当强悍了，但是大模型没有记忆，也不会主动访问外部资源或者调用外部工具。为此，我们在打造智能体应用时，可以通过调用或自定义工具等方式对大模型进行增强。我们举一个简单的例子，让大模型计算 a 的 b 次幂（a 和 b 都是整数），如果 a 和 b 的值很大，大模型很有可能产生幻觉（你可以在网页中用 DeepSeek 试一试），或者用科学计数法给出一个近似的结果，如果我们需要更为精确的结果，可以用自定义的工具来计算 a 的 b 次幂。

```python
from langchain.tools import tool


@tool
def thy_power(a: int, b: int) -> int:
    """计算a的b次幂"""
    return a ** b
```

创建 Agent 对象。

```python
from langchain.agents import create_agent

agent = create_agent(
    model=llm,
    tools=[thy_power],
    system_prompt='你是一个严谨的助手，只在做求幂运算时调用自定义工具，其他运算自行处理',
)
```

调用 Agent 获取结果。

```python
result = agent.invoke({
    'messages': [{'role': 'user', 'content': '计算123的321次幂'}]
})
print(result)
```

上面输出的`result`是一个字典，从这个字典中的`messages`键中，我们可以找到跟大模型对话的消息（`Message`对象），其中有一条是一个`AIMessage`对象，通过它的`tool_calls`属性我们可以看到，自定义的函数被调用了，而且大模型获得了函数运算的结果。如果需要直接获得大模型给出的结果，需要取对话消息中的最后一个`Message`对象，它也是一个`AIMessage`对象，它的`content`属性就是最终的答案，完整的代码如下所示。

```python
from dotenv import load_dotenv
from langchain.agents import create_agent
from langchain_openai import ChatOpenAI
from langchain.tools import tool


@tool
# {
#     "name": "thy_power",
#     "description": "计算a的b次幂",
#     "parameters": {
#         "type": "object",
#         "properties": {
#             "a": {"type": "integer"},
#             "b": {"type": "integer"}
#         },
#         "required": ["a", "b"]
#     }
# }
def thy_power(a: int, b: int) -> int:
    """计算a的b次幂"""
    return a ** b


load_dotenv()
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/v1',
    model='deepseek-v4-flash',
    temperature=0.2,
    max_tokens=4096,
)
agent = create_agent(
    model=llm,
    tools=[thy_power],
    system_prompt='你是一个严谨的助手，只在做求幂运算时调用自定义工具，其他运算自行处理',
)
print(type(agent))
result = agent.invoke({
    'messages': [{'role': 'user', 'content': '计算123的321次幂'}]
})
print(result['messages'][-1].content)
```

> **说明**：上面代码中的系统提示词，我们刻意强调了“只在做求幂运算时调用自定义工具，其他运算自行处理”，这是确保大模型在做求幂运算能够调用自定义工具函数，其他运算则不需要调用工具函数，否则其他的运算也会交给我们自定义的 `thy_power` 函数，结果就可想而知了。

到这里，大模型已经不只是简单的直接回答问题，而是在分析问题并决定是否调用工具，如果需要就执行工具并综合结果给出答案。当然，LangChain 内置了诸多可以使用的 Agent，我们通过下面的例子加以说明。

```python
from dotenv import load_dotenv
from langchain_experimental.agents.agent_toolkits import create_python_agent
from langchain_experimental.tools import PythonREPLTool
from langchain_openai import ChatOpenAI

load_dotenv()
llm = ChatOpenAI(
    base_url='https://api.deepseek.com/',
    model='deepseek-v4-flash',
    temperature=0
)
agent = create_python_agent(
    llm=llm,
    tool=PythonREPLTool(),
    verbose=True,
    agent_executor_kwargs={
        'handle_parsing_errors': True
    }
)
result = agent.invoke('一个小孩爬楼梯，一次可以爬1个台阶、2个台阶或者3个台阶，问爬完10个台阶共有多少种走法？')
print(result)
result = agent.invoke('计算 123 的 321 次幂，给出完整的结果，不要科学计数法。')
print(result)
```

#### 3.5 检索器和RAG

我们再来看看 LangChain 提供的外部文档加载功能，这是做 RAG（检索增强生成）的基础。

```python
from langchain_community.document_loaders import TextLoader, PyPDFLoader, Docx2txtLoader, WebBaseLoader, WikipediaLoader

loader = TextLoader('颐和园.txt')
docs = loader.load()
for doc in docs:
    print(doc.page_content)

loader = PyPDFLoader('提示词工程.pdf')
docs = loader.load()
for i, doc in enumerate(docs):
    print('~' * 10, i, '~' * 10)
    print(doc.page_content)

loader = Docx2txtLoader('数据分析概述.docx')
docs = loader.load()
for i, doc in enumerate(docs):
    print('~' * 10, i, '~' * 10)
    print(doc.page_content)

loader = WebBaseLoader('https://movie.douban.com/top250')
docs = loader.load()
for i, doc in enumerate(docs):
    print('~' * 10, i, '~' * 10)
    print(doc.page_content)

loader = WikipediaLoader(
    query='颐和园',
    lang='zh',
    load_max_docs=1,
    doc_content_chars_max=20000
)
docs = loader.load()
for i, doc in enumerate(docs):
    print('~' * 10, i, '~' * 10)
    print(doc.page_content)
```

对于加载的文档，我们可以做切割并保存到项目数据库中，将来可以让大模型根据我们自己加载的文档来生成答案，降低幻觉，这就是所谓的 RAG。

```python
from dotenv import load_dotenv
from langchain_ollama import OllamaEmbeddings
from langchain_openai import ChatOpenAI
from langchain_community.vectorstores import Chroma
from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.runnables import RunnablePassthrough

load_dotenv()

# 1. 加载文档
loader = TextLoader('颐和园.txt', encoding='utf-8')
docs = loader.load()

# 2. 切割文档
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    separators=['\n\n', '\n', '。', '！', '？', '；', '，', ' ', ''],
)
texts = splitter.split_documents(docs)

# 3. 词嵌入模型 ---> 将中文句子变成向量
embeddings = OllamaEmbeddings(model='nomic-embed-text')

# 4. 向量数据库
vectorstore = Chroma.from_documents(
    documents=texts,
    embedding=embeddings,
    persist_directory="./chroma_db"  # 向量数据本地持久化路径
)
retriever = vectorstore.as_retriever(search_kwargs={"k": 3})

# docs = retriever.invoke('颐和园有没有受到八国联军的破坏？')
# for doc in docs:
#     print(doc.page_content)
#     print('~' * 50)

# 5. 大模型+提示词
llm = ChatOpenAI(
    base_url="https://api.deepseek.com",
    model="deepseek-v4-flash",
    temperature=0
)

prompt = ChatPromptTemplate.from_template(
    '你是一个知识问答助手，请根据提供的上下文回答问题。如果你不知道，就说不知道。\n'
    '上下文：\n{context}'
    '问题：\n{question}'
)

# 6. RAG链
handler = {
    "context": retriever | (lambda docs: '\n\n'.join([doc.page_content for doc in docs])),
    "question": RunnablePassthrough()
}
rag_chain = handler | prompt | llm
resp = rag_chain.invoke('介绍下买卖街')
print(resp)
```

> **说明**：上面的代码用到了本地 Ollama 部署的 `nomic-embed-text` 模型，`nomic-embed-text` 是目前开源 Embedding 模型里面最受欢迎的模型之一。本地安装 Ollama 以后，可以通过命令 `ollama pull nomic-embed-text` 下载该模型， 启动 Ollama 服务后就可以使用它。

## 4. 总结

到这里，我们对 LangChain 已经有了一个简单的认知，它不是魔法，而是把你之前重复书写过的 AI 应用模式进行了系统化封装。
